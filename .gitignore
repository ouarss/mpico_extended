build
pico-examples
pico-sdk
.vscode

# Third-party dependency (aic_pico), fetched separately (see build.sh).
# Not vendored here: it stays the upstream author's work, and this repository
# keeps no link to any upstream remote.
modules/

# Build artefact (regenerated by build.sh, published as a release asset)
mpico_extended.uf2

# Custom mai_pico board: kept local, not republished. It is upstream's design
# with the output reworked, so the production-ready Gerbers stay theirs to
# hand out; the preview picture in custom-build/PCBs/ shows what changed.
custom-build/PCBs/mai_pico-custom-output.zip
